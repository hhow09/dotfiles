[core]
    ignorecase = false
[alias]
    aa = add --all :/
    b = branch
	last = log -1 HEAD
	c = commit -m
    po = "!git push --set-upstream origin \"$(git rev-parse --abbrev-ref HEAD)\""
    # push current branch to remote